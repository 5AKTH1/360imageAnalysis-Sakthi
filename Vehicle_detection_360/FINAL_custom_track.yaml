tracker_type: botsort 
track_high_thresh: 0.4    
track_low_thresh: 0.1     
new_track_thresh: 0.5
track_buffer: 90          
match_thresh: 0.8         
fuse_score: True          # Whether to fuse confidence scores with IoU distances
# --------------------------------------
gmc_method: orb           
proximity_thresh: 0.5
appearance_thresh: 0.25
with_reid: False

#best until now
#tracker_type: bytetrack 
#track_high_thresh: 0.4    # Only start a track if its clear
#track_low_thresh: 0.1     # But keep it alive even if it gets blurry
#new_track_thresh: 0.5
#track_buffer: 120         # Keep IDs alive for 6 seconds (crucial for traffic)
#match_thresh: 0.7          # Balanced threshold
#fuse_score: True 


